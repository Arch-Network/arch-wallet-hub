services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: arch_wallet_hub
    ports:
      - "5432:5432"
    volumes:
      - arch_wallet_hub_pg:/var/lib/postgresql/data

volumes:
  arch_wallet_hub_pg: {}
