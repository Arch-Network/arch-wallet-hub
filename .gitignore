node_modules
.env
dist

# Cursor / IDE
.specstory/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env.*
!.env.example

# External SDK checkout (not a proper submodule)
arch-typescript-sdk/

# CDK
infra/cdk/cdk.out/
infra/cdk/node_modules/
# cdk.context.json caches account-specific AZ/AMI data; recreate via
# `cdk synth` rather than committing.
infra/cdk/cdk.context.json
